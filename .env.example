# AIHub 环境变量模板
#
# 本文件是「唯一键名清单」：新增变量必须同步登记（AGENTS.md §6）。
# 用法：复制为 .env 后填入真实值；.env 不入版本库。

# --- 密钥 ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GITHUB_TOKEN=

# --- 路径 ---
# 本仓库根路径。仓库内部的其余路径由脚本从它派生，不单独登记。
AIHUB_HOME=C:\Work\AIHub

# 运行时数据外置目录（会话状态、缓存、日志、记忆）。
# 位于仓库之外，无法从 AIHUB_HOME 推导，因此必须独立定义。
AIHUB_DATA=C:\Work\AIHub_Data

# 说明：AIHUB_SKILLS 不在此登记。
# 它是 AIHUB_HOME 的派生值（<AIHUB_HOME>/shared/skills），
# 由 scripts/lib/env.py 计算 —— 派生值不是变量，登记它等于制造第二份事实源。
